// Keys for localStorage so we don't typo them in different files
export const AUTH_TOKEN_KEY = 'auth_token';
export const AUTH_USER_KEY = 'auth_user';
export const CART_SESSION_KEY = 'cart_session_id';
